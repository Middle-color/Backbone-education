#page-login.container.page-login
  .row
    .col-xs-12
      form.form-signin(role='form', novalidate='novalidate')
        .form-group
          h2.form-signin-heading Please sign in to join Chat
        .form-group
          input.form-control.login(type='text', placeholder='Nickname', autofocus='autofocus')
        .form-group
          input.form-control.password(type='password', placeholder='Password')
        .form-group
          button.btn.btn-primary.btn-block(type='submit') Sign in